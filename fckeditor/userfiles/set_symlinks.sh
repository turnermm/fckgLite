#!/bin/sh
# run this from the command line in the current directory (userfiles): ./set_symlinks.sh
# this script must be set to executable and it assumes that your data edirectory has not been moved
ln -s ../../../../../data/media file
ln -s ../../../../../data/media image
ln -s ../../../../../data/media media
ln -s ../../../../../data/media flash

